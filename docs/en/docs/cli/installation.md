# Installation

You can install it using ```uv``` referencing the repo:

```bash
uv tool install git+https://github.com/walkai-org/walkai-cli
```

Or you can install it as editable:

```bash
uv tool install --editable .
```